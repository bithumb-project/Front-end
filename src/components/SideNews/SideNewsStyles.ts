import styled from 'styled-components';
import { ListItemText } from '@mui/material';

export const NewsItemText = styled(ListItemText)`
  & .MuiTypography-root-bWuLxc {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
`;