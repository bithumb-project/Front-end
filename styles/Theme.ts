import { createTheme } from '@mui/material';

const Theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#929292',
      light: '#dadada',
    },
  },
});

export default Theme;
